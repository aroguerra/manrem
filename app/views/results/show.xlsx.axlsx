wb = xlsx_package.workbook

wb.add_worksheet(name: "Items") do |sheet|
  # Create the header row
  sheet.add_row ["Item Name", "Quantity"]
  # Create entries for each item
  @results.each do |item|
    sheet.add_row [item.period, item.price]
  end
end
